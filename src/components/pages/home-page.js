import React from "react";
import BookList from "../book-list";

const HomePage = (props) => {
  
  return (
    <div className="page">
      <BookList />
      
    </div>
  );
};

export default HomePage;
